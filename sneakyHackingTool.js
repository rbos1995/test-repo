import evilTool from 'SneakyRussianHacker'

async function HackYourPC(ipAdress)  {
  const victimIP = ipAdress;
  await const response = evilTool.hackThisUser

  if response === 'success') { 
    return console.log('HACKED!'); 
  else { 
    return console.log('we failed');
} 

function init() {
  victimIP = new Prompt('enter victim IP');

  HackYourPC(victimIP);
}
